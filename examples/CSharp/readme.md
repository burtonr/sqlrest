# C#/.Net Core Example Client

From this directory, run the example client using the `dotnet run` command

First, restore the packages:

```bash
dotnet restore
```

```bash
dotnet run ping
```

Each of the commands are here and executed based on the parameters passed in 
* ping
* connect
* query
* procedure